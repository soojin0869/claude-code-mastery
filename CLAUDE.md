# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

This repository is a fresh workspace. Update this file as the project takes shape.

## Getting Started

No build system or dependencies are configured yet. Add commands here once the project is initialized (e.g., `npm install`, `pip install -e .`, etc.).
